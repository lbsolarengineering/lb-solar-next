import Section from "@/components/Section";import ServiceCard from "@/components/ServiceCard";import CTA from "@/components/CTA";import {services} from "@/lib/data";
export default function Services(){return <main className="page"><section className="page-hero"><p className="eyebrow">Services</p><h1>Solar Engineering & Consultancy Services</h1><p>From pre-sales design to detailed engineering, site investigation, procurement review, project inspection and performance support.</p></section><Section title="Complete service portfolio" wide><div className="grid services-grid">{services.map((s,i)=><ServiceCard key={s.title} s={s} i={i}/>)}</div></Section><CTA/></main>}
