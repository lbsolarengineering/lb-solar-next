export default function Section({eyebrow,title,children,wide=false}:{eyebrow?:string;title:string;children:React.ReactNode;wide?:boolean}){return <section className={wide?"section wide":"section"}>{eyebrow&&<p className="eyebrow">{eyebrow}</p>}<h2>{title}</h2>{children}</section>}
